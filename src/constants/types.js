// @flow

export type ColorsType = {
    default: boolean,
    primary: boolean,
    success: boolean,
    info: boolean,
    warning: boolean,
    danger: boolean
};


export type SizesType = {
    normal: boolean,
    small: boolean,
    large: boolean
};
